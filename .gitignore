node_modules/
dist/
server/data/
*.db
*.db-wal
*.db-shm
.env
.env.local
.DS_Store

# Personal deployment scripts (contain keys/credentials — kept local)
deploy-pi.sh
update-hermes.sh

# Local Claude Code settings (per-machine permission allowlist, not project config)
.claude/settings.local.json
